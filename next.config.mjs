/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: "standalone",
    images: {
        domains: ['prece66-01-storage.cloud.ce.kmitl.ac.th'],
    },
};

export default nextConfig;
